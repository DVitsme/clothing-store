const Loading = () => {
  return (
    <div className="animate-spin inline-block w-12 h-12 border-8 border-t-8 rounded-full border-t-blue-500">
      <span className="sr-only">Loading...</span>
    </div>
  );
};

export default Loading;
